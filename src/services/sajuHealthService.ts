export function getSajuHealthText(
    dayMasterWeakness: string,
    weakestElementName: string
  ) {
    return `${dayMasterWeakness}
  
  건강 관리 포인트는 부족한 오행인 ${weakestElementName} 기운을 보완하는 방향으로 잡는 것이 좋습니다. 무리한 생활 패턴보다 수면, 식사, 스트레스 관리의 균형을 우선하세요.`;
  }